import { Routes } from '@angular/router';
import { CreateCountryComponent } from './create-country/create-country.component'; // Import the component

export const routes: Routes = [
    {
        path: "", 
        component: CreateCountryComponent 
    }
];
